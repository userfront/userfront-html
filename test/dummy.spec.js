import Test from "./config/test.utils.js";
import myObj from "@anymod/core";

describe("it should do", () => {
  it("should do", () => {
    console.log("core", myObj);
    expect(true).toEqual(false);
  });
});
